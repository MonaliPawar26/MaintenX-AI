# Frontend Assets

Place any static assets here:
- `favicon.ico` — browser tab icon
- `logo.png` — brand logo
- `og-image.png` — Open Graph social preview image (1200x630)

All fonts and CDN resources are loaded directly from Google Fonts and cdnjs in `index.html`.
